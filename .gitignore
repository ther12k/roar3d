# Godot generated cache
.godot/
*.translation

# Export artifacts
export/
build/
*.apk
*.aab
*.keystore
*.jks
secrets/

# OS noise
.DS_Store
Thumbs.db

# Test scratch output (tests write to a temp dir, but just in case)
*.tmp.json

# Local visual evidence (published as release assets instead)
docs/evidence/*.png
build/
keystore/*.keystore
keystore/*.jks
